<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tutorial.mpdemo.mapper.UserMapper">
    <select id="selectRaw" resultType="com.tutorial.mpdemo.entity.User">
        select * from user where age > 25 and age != 35 or name like 'lig'
    </select>
</mapper>